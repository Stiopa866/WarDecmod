declarewar = {
	condition = { potential = { always = no } }
	effect = {
		if = {
			limit = {
				ROOT = { ai = no }
			}
			hidden_effect = { wardec_control_effect = yes }
			custom_tooltip = wardec_bruh_moment
		}
	}
}

threaten_war = {
	condition = { potential = { always = no } }
	effect = {
		if = {
			limit = {
				ROOT = { ai = no }
			}
			hidden_effect = { wardec_control_effect = yes }
			custom_tooltip = wardec_bruh_moment
		}
	}
}